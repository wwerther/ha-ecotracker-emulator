{
  "config": {
    "step": {
      "user": {
        "title": "Set up EcoTracker Emulator",
        "description": "The mDNS service is announced as `{prefix}<MAC>`. The MAC suffix should start with the EcoTracker OUI `{oui}` for best client compatibility – a random suggestion is pre-filled. Serial and product ID are sent as TXT records.",
        "data": {
          "mac_suffix": "MAC suffix (12 hex chars, no separators)",
          "serial": "Serial (12 hex chars)",
          "product_id": "Product ID",
          "port": "Port (default 8123)"
        }
      }
    },
    "error": {
      "invalid_mac_suffix": "MAC suffix must be exactly 12 hexadecimal characters (separators are stripped automatically).",
      "invalid_serial": "Serial must be exactly 12 hexadecimal characters.",
      "invalid_product_id": "Product ID must not be empty.",
      "invalid_port": "Port must be between 1 and 65535."
    },
    "abort": {
      "already_configured": "This service name is already in use.",
      "single_instance_allowed": "Only one EcoTracker Emulator instance can be configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Map JSON fields to sensors",
        "description": "For each field of the EcoTracker `/v1/json` response you can pick a Home Assistant sensor whose state will be served. If no sensor is set – or its state is unknown/unavailable – the numeric fallback is used. Energy counters are in watt-hours (Wh). The sensor pickers are pre-filtered by `device_class` and matching units (W/kW for power, Wh/kWh for energy); enable *Show all sensors* to disable that filter.",
        "data": {
          "show_all_sensors": "Show all sensors (disable device_class / unit filter)",
          "power_entity": "power – sensor",
          "power_fallback": "power – fallback (W)",
          "powerAvg_entity": "powerAvg – sensor",
          "powerAvg_fallback": "powerAvg – fallback (W)",
          "agePower_entity": "agePower – sensor",
          "agePower_fallback": "agePower – fallback (ms)",
          "powerPhase1_entity": "powerPhase1 – sensor",
          "powerPhase1_fallback": "powerPhase1 – fallback (W)",
          "powerPhase2_entity": "powerPhase2 – sensor",
          "powerPhase2_fallback": "powerPhase2 – fallback (W)",
          "powerPhase3_entity": "powerPhase3 – sensor",
          "powerPhase3_fallback": "powerPhase3 – fallback (W)",
          "energyCounterOut_entity": "energyCounterOut – sensor",
          "energyCounterOut_fallback": "energyCounterOut – fallback (Wh)",
          "energyCounterIn_entity": "energyCounterIn – sensor",
          "energyCounterIn_fallback": "energyCounterIn – fallback (Wh)"
        }
      }
    }
  }
}
